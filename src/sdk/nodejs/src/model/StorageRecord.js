/*
 * self-managed-osdu
 * Rest API Documentation for Self Managed OSDU
 *
 * OpenAPI spec version: 0.11.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.22
 *
 * Do not edit the class manually.
 *
 */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['ApiClient', 'model/StorageAcl', 'model/StorageLegal', 'model/StorageRecordHistory'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'), require('./StorageAcl'), require('./StorageLegal'), require('./StorageRecordHistory'));
  } else {
    // Browser globals (root is window)
    if (!root.SelfManagedOsdu) {
      root.SelfManagedOsdu = {};
    }
    root.SelfManagedOsdu.StorageRecord = factory(root.SelfManagedOsdu.ApiClient, root.SelfManagedOsdu.StorageAcl, root.SelfManagedOsdu.StorageLegal, root.SelfManagedOsdu.StorageRecordHistory);
  }
}(this, function(ApiClient, StorageAcl, StorageLegal, StorageRecordHistory) {
  'use strict';

  /**
   * The StorageRecord model module.
   * @module model/StorageRecord
   * @version 0.11.0
   */

  /**
   * Constructs a new <code>StorageRecord</code>.
   * Storage record
   * @alias module:model/StorageRecord
   * @class
   * @param kind {String} Kind of data it is being ingested. Must follow the naming convention: {OSDU-Account-Id}:{dataset-name}:{record-type}:{version}.
   * @param acl {module:model/StorageAcl} Group of users who have access to the record.
   * @param legal {module:model/StorageLegal} Attributes which represent the legal constraints associated with the record.
   * @param data {Object.<String, Object>} Record payload represented as a list of key-value pairs.
   */
  var exports = function(kind, acl, legal, data) {
    this.kind = kind;
    this.acl = acl;
    this.legal = legal;
    this.data = data;
  };

  /**
   * Constructs a <code>StorageRecord</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/StorageRecord} obj Optional instance to populate.
   * @return {module:model/StorageRecord} The populated <code>StorageRecord</code> instance.
   */
  exports.constructFromObject = function(data, obj) {
    if (data) {
      obj = obj || new exports();
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'String');
      if (data.hasOwnProperty('kind'))
        obj.kind = ApiClient.convertToType(data['kind'], 'String');
      if (data.hasOwnProperty('acl'))
        obj.acl = StorageAcl.constructFromObject(data['acl']);
      if (data.hasOwnProperty('legal'))
        obj.legal = StorageLegal.constructFromObject(data['legal']);
      if (data.hasOwnProperty('data'))
        obj.data = ApiClient.convertToType(data['data'], {'String': Object});
      if (data.hasOwnProperty('history'))
        obj.history = StorageRecordHistory.constructFromObject(data['history']);
    }
    return obj;
  }

  /**
   * Unique identifier in whole Data Lake. When not provided, Data Lake will create and assign an id to the record. Must follow the naming convention: {OSDU-Account-Id}:{object-type}:{uuid}.
   * @member {String} id
   */
  exports.prototype.id = undefined;

  /**
   * Kind of data it is being ingested. Must follow the naming convention: {OSDU-Account-Id}:{dataset-name}:{record-type}:{version}.
   * @member {String} kind
   */
  exports.prototype.kind = undefined;

  /**
   * Group of users who have access to the record.
   * @member {module:model/StorageAcl} acl
   */
  exports.prototype.acl = undefined;

  /**
   * Attributes which represent the legal constraints associated with the record.
   * @member {module:model/StorageLegal} legal
   */
  exports.prototype.legal = undefined;

  /**
   * Record payload represented as a list of key-value pairs.
   * @member {Object.<String, Object>} data
   */
  exports.prototype.data = undefined;

  /**
   * Record ancestry information.
   * @member {module:model/StorageRecordHistory} history
   */
  exports.prototype.history = undefined;


  return exports;

}));
